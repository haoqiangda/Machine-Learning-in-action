第一步：将邮件的文本划分成词汇，在这里我们认为长度小于2的默认为不是词汇，过滤掉即可。
        返回的是"一串"小写的拆分后的邮件信息。


第二步：文档"词袋"模型（记录每个词汇出现的次数）：使用数组代替集合数据结构，可以保存词汇"频率"的信息。


第三步：输入正常邮件25封和25封垃圾邮件。并从50封邮件中”随机“选取10封作为测试样本，剩下的40封作为训练样本。
        训练模型：40封训练样本，训练出先验概率和条件概率
        测试模型：遍历10个测试样本，计算垃圾邮件分类的正确率。